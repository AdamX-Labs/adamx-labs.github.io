# AdamX Labs

The public landing page for AdamX Labs, the research and development division of Horizon Synergy.

This is a dependency-free static site. Open `index.html` directly, or serve the directory locally:

```sh
python3 -m http.server 8000
```
